<div class="wrapper vh-100">
  <h1 class="text-center p-4">Cocktail's details</h1>
  <div class="container card-cocktail">
    <h2 class="text-center p-3"><%= @cocktail.name %></h2>
    <div class="row align-items-center">
      <div class="col text-center">

        <%= image_tag "cocktail5.jpg", height: '300px' %>
      </div>
      <div class="col">
        <p class="card-content">
          <% @cocktail.doses.each do |dose| %>
          <%= dose.description %> of <%= dose.ingredient.name %>
          <%= link_to dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } do %>
          <i class="fa fa-trash"></i>
          <% end %>
          <br>
        </p>
        <% end %>
        <%= link_to 'Add an Ingredient', new_cocktail_dose_path(@cocktail), class: 'btn btn-warning' %>

        <h4 class="card-text"><small class="text-muted"><br>"the perfect cocktail for a perfect party"</small></h4>
      </div>
    </div>
  </div>
  <div class="text-center m-5">
    <%= link_to cocktails_path, class: 'btn btn-info' do %>
    <i class="fas fa-long-arrow-alt-left"></i> Back to cocktails list
    <% end %>
  </div>
</div>
